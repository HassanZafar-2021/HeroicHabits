{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2022" /* Set the JavaScript language version for emitted JavaScript */,
    "module": "ESNext" /* Specify what module code is generated (use ESNext for modern JS features) */,
    "moduleResolution": "node" /* Use 'node' resolution strategy (common in backend or Node.js projects) */,
    "skipLibCheck": true /* Skip type checking of declaration files (improves build time) */,

    /* JavaScript Support */
    "allowJs": true /* Allow JavaScript files in the project */,
    "checkJs": false /* Optionally enable type-checking in JS files (typically false if working with TypeScript only) */,

    /* Interop Constraints */
    "esModuleInterop": true /* Ensures compatibility with CommonJS modules */,
    "allowSyntheticDefaultImports": true /* Allow default imports from modules that don't have a default export */,
    "forceConsistentCasingInFileNames": true /* Ensure consistent casing in file imports */,

    /* Type Checking */
    "strict": true /* Enable strict type-checking options */,
    "noUnusedLocals": true /* Report unused local variables */,
    "noUnusedParameters": true /* Report unused function parameters */,
    "noFallthroughCasesInSwitch": true /* Report fallthrough in switch cases */,
    "exactOptionalPropertyTypes": true /* Preserve exact optional property types */,
    "noImplicitReturns": true /* Ensure all functions return a value */,
    "noImplicitOverride": true /* Ensure overridden methods are marked with 'override' */,
    "useUnknownInCatchVariables": true /* Use 'unknown' type for catch clause variables (to force type checking) */,

    /* Directories and Output */
    "outDir": "./dist" /* Output directory for compiled JS files */,
    "rootDir": "./src" /* Source directory for TypeScript files (set if not using the default structure) */,

    /* Enable additional features */
    "resolveJsonModule": true /* Allow importing JSON modules */,
    "skipDefaultLibCheck": true /* Skip type checking of default libraries (improves build time) */,

    /* Other configurations */
    "lib": [
      "ES2020",
      "DOM"
    ] /* Include appropriate libraries for the project (e.g., DOM for browser environments) */,
    "isolatedModules": true /* Ensures each file is treated as a separate module for better compatibility */
  },

  /* Includes and Excludes */
  "include": ["src/**/*"],

  "exclude": ["node_modules"] /* Exclude 'node_modules' from being compiled */
}
